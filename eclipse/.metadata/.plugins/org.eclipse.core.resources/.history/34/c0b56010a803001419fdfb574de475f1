package tutorial.DMAexample;

import tutorial.DMAexample.MyItems.DeathBallEntity;
import cpw.mods.fml.client.registry.RenderingRegistry;
import cpw.mods.fml.common.registry.EntityRegistry;

public class CommonProxy {

        // Client stuff
        public void registerRenderers() {
        	EntityRegistry.registerModEntity(DeathBallEntity.class, "fireProjectile", 1, YourModsName.instance, 128, 5, true);
                // Nothing here as the server doesn't render graphics or entities!
        	 //RenderingRegistry.registerEntityRenderingHandler(DeathBallEntity.class, new DeathBallEntity(null));
        }
}